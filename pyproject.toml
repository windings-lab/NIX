[project]
name = "nix"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jsonschema>=4.25.1",
    "lxml>=6.0.2",
    "pytest>=9.0.1",
    "requests>=2.32.5",
]

[pytest]
testpaths = ["tests"]
